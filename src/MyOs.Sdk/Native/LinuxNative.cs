namespace MyOs.Sdk.Native;

internal static class LinuxNative
{
    // Native Linux ABI wrappers live here when the SDK needs them. Keep this
    // small and internal until a higher-level SDK API requires each syscall.
}
